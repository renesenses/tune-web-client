/** Commandes réactives du vrai composant monté par le témoin #3818. */
export function visualizerControls3818() {
  const controls = $state({ playing: true, mode: 'waveform' as 'waveform' | 'spectrum' });
  return controls;
}
